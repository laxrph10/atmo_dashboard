<% flash.each do |name, msg| %>
    <div class="flash">
    <%= content_tag :div, class: "alert alert-info alert-dismissable", id: "flash_#{name}" do-%>
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
      <%=msg%>
      <%end%>
      </div>
<% end %>

